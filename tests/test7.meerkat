service test7 {

  var x = 1;
  def y = x + 2;
  var z = "hello";

  table sample {
    id: number, 
    name: string, 
    age: number, 
    };

  def b = select from sample where sample.id == 1 && sample.name == "Sunay" ;

  def act = action { insert {id: 1, name: "Sunay", age: 18, } into sample };
  def act2 = action { insert {id: 2, name: "A", age: 18, } into sample };

}

@test(test7) {
    do act;
    assert(x==1);
    // assert(b == [{id: 1, name: "Sunay", age: 18}]);    // todo (think about testing select statements)
    do act2;
}