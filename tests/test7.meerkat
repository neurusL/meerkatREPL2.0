service test7 {

  var x = 1;
  def y = x + 2;
  var z = "hello";

  def a = if x > 2 && x>0 then 2 else 0;
  table sample {id: number, name: string, age: number, };

  def b = select from sample where sample.id == 1 && sample.name == "Sunay" ;

  var x = sample.id * 2;

  //def act = action { insert {id: 1, name: "Sunay", age: 18, } into sample };

}

@test(test7) {
    //do act;
    assert(a==2);
}